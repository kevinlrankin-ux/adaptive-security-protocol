# Adaptive Security Protocol (ASP)

ASP is a **protocol**, not a program.

It defines:
- non-removable invariants,
- constrained response verbs,
- and governance rules for security escalation.

ASP is implemented inside systems, not installed as software.
